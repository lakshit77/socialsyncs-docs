The OAuth2 Redirect URI is the location where the provider will redirect to after a successful login. This needs to be set to your SocialSyncs `FRONTEND_URL` + `/integrations/social/` + `{provider}`.

You only need to set one OAuth2 Redirect URI when you are setting up your SocialSyncs app.

